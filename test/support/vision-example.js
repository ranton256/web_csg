// The bored-cube example from vision.md, used by tests across milestones. The
// app ships it as a built-in example (M6), so it lives in src/ui/examples.js.
export { BORED_CUBE_SOURCE as VISION_EXAMPLE } from '../../src/ui/examples.js';
